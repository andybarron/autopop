const util = require('jsmidgen').Util;

module.exports = {
  generate: function(track, root, chords, rhythm) {
    for (const [index, duration] of rhythm) {
      track.addChord(0, chords[index].map(n => n + root), duration);
    }
  },
};
